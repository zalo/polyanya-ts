import { createRoot } from "react-dom/client"

createRoot(document.getElementById("root")!).render(
  <div style={{ padding: 32, fontFamily: "system-ui" }}>
    <h1>Polyanya Demo</h1>
    <p>Use React Cosmos to view the interactive fixtures.</p>
  </div>,
)
